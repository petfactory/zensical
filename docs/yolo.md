---
icon: lucide/rocket
---

# WAZAAAA

For full documentation visit [zensical.org](https://zensical.org/docs/).

## Commands
 
It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.

## Unreal

It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.
It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.
It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.

## Houdini

It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical. node wrangle vdvsdv in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.It is likely that you will want to run some initialization code in your JavaScript once the page has been 
vex in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.It is likely that you will want to run some initialization code in your JavaScript once the page has been fully loaded by the browser. This means installing a callback function subscribing to events on the document$ observable exported by Zensical.It is likely that you will want to run some initialization code in your JavaScript once the page has been 